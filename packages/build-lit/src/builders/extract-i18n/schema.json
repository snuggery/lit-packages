{
	"$schema": "http://json-schema.org/draft-07/schema",
	"type": "object",
	"properties": {
		"applicationTarget": {
			"type": "string",
			"description": "An application builder target for which to look for translations, in the format of `target` or `[project]:target[:configuration]`. You can also pass in more than one configuration name as a comma-separated list. Example: `project:target:production,staging`.",
			"default": "build"
		}
	},
	"additionalProperties": false
}
