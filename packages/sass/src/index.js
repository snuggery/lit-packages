/**
 * @returns {import('lit').CSSResult}
 */
export function sass() {
	throw new Error(
		'The sass template tag function should be replaced at build time',
	);
}

/**
 * @returns {import('lit').CSSResult}
 */
export function scss() {
	throw new Error(
		'The scss template tag function should be replaced at build time',
	);
}
